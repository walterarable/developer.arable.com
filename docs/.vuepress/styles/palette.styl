// The palette.styl is responsible for global color settings.
// During compilation, theme color constants should be resolved by the
// preprocessor first and then be applied to the global context.

// showing default values
$accentColor = #1773c6
$textColor = #2c3e50
$borderColor = #eaecef
$codeBgColor = #282c34

// h3
//     color: #4FAF4C

// Layout configuration
// $navbarHeight = 3.6rem
// $sidebarWidth = 20rem
// $contentWidth = 740px